import axios from 'axios'

import { REMOVE_REGION, LIST_REGIONS, UPDATE_REGION, CREATE_REGION } from './types'

export const removeRegion = () => dispatch => {

}

export const listRegions = () => dispatch => {
    
}

export const updateRegion = () => dispatch => {
    
}

export const createRegion = () => dispatch => {
    
}